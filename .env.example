# Travel Log Environment Configuration

# Database (automatically configured in docker-compose)
DATABASE_URL=postgresql://travellog:travellog@postgres:5432/travellog

# OpenAI API Key for AI-powered summaries
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# AI Provider (currently only openai is supported)
AI_PROVIDER=openai

# Server port
PORT=3000